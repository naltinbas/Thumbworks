import 'package:flutter/material.dart';

/// The sham's colours: a card table at night, Ash's coins in gold and
/// Birch's in silver, the chain of chances in bronze with the fair line
/// faint behind it.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const baize = Color(0xFF1E2A22);
  static const ash = Color(0xFFD1A85E);
  static const ashDark = Color(0xFF8C6E3E);
  static const birch = Color(0xFFB9BCC4);
  static const birchDark = Color(0xFF6E717A);
  static const chain = Color(0xFFC77A4A);
  static const fairLine = Color(0xFF3E3A46);
  static const post = Color(0xFF4A4650);
  static const here = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
