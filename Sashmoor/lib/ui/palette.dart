import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF171A1D);
  static const board = Color(0xFF21262B);
  static const line = Color(0xFF32393F);

  /// The sash and its lights.
  static const frame = Color(0xFF6E5A44);
  static const light = Color(0xFF23282E);

  /// The glass.
  static const glass = Color(0xFF9BC4DB);
  static const glassRim = Color(0xFF5E86A0);
  static const glint = Color(0xFFD8ECF7);

  /// A framed window, called out.
  static const window = Color(0xFFC96A4A);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
