import 'package:flutter/material.dart';

/// The sham's colours: a card table in green baize, cards cream face
/// up with ink letters and patterned blue face down, the pattern asked
/// in outline above, green when it comes and rust when it never can.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const baize = Color(0xFF2E5A3A);
  static const baizeDark = Color(0xFF244830);
  static const face = Color(0xFFF2EBD8);
  static const pen = Color(0xFF2A2420);
  static const back = Color(0xFF3B5A9A);
  static const backLine = Color(0xFF6D8AC8);
  static const edge = Color(0xFF8A836F);
  static const chalk = Color(0xFFECEAE2);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
