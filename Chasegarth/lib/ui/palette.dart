import 'package:flutter/material.dart';

/// The colours.
///
/// A composing bench under a lamp: dark wood, a brass chase, and type with
/// paper-coloured faces. The empty cell is the bench showing through.
class Palette {
  const Palette._();

  static const night = Color(0xFF14100C);
  static const verge = Color(0xFF1F1913);
  static const line = Color(0xFF2E2519);
  static const edge = Color(0xFF4A3C28);

  static const brass = Color(0xFF9A7E46);
  static const type = Color(0xFF3A2F21);
  static const face = Color(0xFFEDE3CE);
  static const placed = Color(0xFFB9C7A5);
  static const bench = Color(0xFF191410);

  static const ink = Color(0xFFEFE8DB);
  static const inkDim = Color(0xFF94897A);
  static const good = Color(0xFF9CBB6D);
  static const bad = Color(0xFFD9736A);
}
