import 'package:flutter/material.dart';

/// The sham's colours: a cellar by lamplight, the casks in wine, the
/// one with the most twos in it lit like brass.
class Palette {
  static const night = Color(0xFF17130F);
  static const board = Color(0xFF221C16);

  static const ink = Color(0xFFEFE7DA);
  static const inkDim = Color(0xFF9C9082);

  static const line = Color(0xFF473A2C);
  static const hoop = Color(0xFF8A5A2B);
  static const wine = Color(0xFF7A2331);
  static const wineLight = Color(0xFFB0455A);
  static const deep = Color(0xFFE0A83C);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
