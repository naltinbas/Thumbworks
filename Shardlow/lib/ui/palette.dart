import 'package:flutter/material.dart';

/// The colours.
///
/// A pottery yard early: grey light, a wooden ladder against the kiln, and
/// the warm terracotta of the pots the only colour with any blood in it.
class Palette {
  const Palette._();

  static const night = Color(0xFF101112);
  static const verge = Color(0xFF1A1C1E);
  static const line = Color(0xFF282B2E);
  static const edge = Color(0xFF42464B);

  static const wood = Color(0xFF6E5B41);
  static const pot = Color(0xFFC07A52);
  static const shard = Color(0xFF8A4B3A);
  static const lived = Color(0xFF8FA98B);
  static const maybe = Color(0xFF39586B);

  static const ink = Color(0xFFECEEEF);
  static const inkDim = Color(0xFF8B9096);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
