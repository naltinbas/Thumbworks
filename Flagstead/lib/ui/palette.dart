import 'package:flutter/material.dart';

/// The sham's colours: a field at dusk, the hall in chalk, the four
/// lines from the peg in two pairs, one gold and one blue.
class Palette {
  static const night = Color(0xFF13161A);
  static const board = Color(0xFF1D2128);

  static const ink = Color(0xFFEBE9E3);
  static const inkDim = Color(0xFF95999F);

  static const line = Color(0xFF363C46);
  static const grass = Color(0xFF19221E);
  static const chalk = Color(0xFFD7D2C4);
  static const post = Color(0xFFE0DACA);
  static const peg = Color(0xFFE8C46A);
  static const oneWay = Color(0xFFE8C46A);
  static const otherWay = Color(0xFF6FA8DC);
  static const gold = Color(0xFFE8C46A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
