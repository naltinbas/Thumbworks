import 'dart:ui';

/// A schoolyard at the end of the day.
class Palette {
  const Palette._();

  /// The yard behind everything.
  static const dusk = Color(0xFF1B1D22);

  /// Panels and cards: the wall bricks.
  static const wall = Color(0xFF262A31);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3A4049);

  /// Borders that mean something.
  static const edge = Color(0xFF5D6672);

  /// Words.
  static const ink = Color(0xFFE9EAE2);

  /// Words standing back.
  static const inkDim = Color(0xFF959C93);

  /// A child still in, one stepped out, and you.
  static const child = Color(0xFFB9C0AC);
  static const gone = Color(0xFF4A4F48);
  static const you = Color(0xFF63B0E3);

  /// The dip stone where the count begins.
  static const stone = Color(0xFF8B8378);

  /// The dipper's finger: where the chant stands now.
  static const finger = Color(0xFFC98F4E);

  /// The safe seat, when the game is asked why, and the figures of the
  /// binary turn.
  static const safeSeat = Color(0xFFE0B34E);

  /// The pointed-at seat when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A dip survived, and one lost.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
