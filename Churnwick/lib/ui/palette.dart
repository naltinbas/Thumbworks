import 'package:flutter/material.dart';

/// The colours.
///
/// A dairy before light: cold stone, brass churns and the only bright thing in
/// the room being the milk itself.
class Palette {
  const Palette._();

  static const night = Color(0xFF0F1315);
  static const verge = Color(0xFF191F23);
  static const line = Color(0xFF262E34);
  static const edge = Color(0xFF3B4650);

  static const churn = Color(0xFF3A454E);
  static const brass = Color(0xFF8C7A4E);
  static const milk = Color(0xFFEFE7D4);
  static const step = Color(0xFF6E7B86);

  static const ink = Color(0xFFE9EDF0);
  static const inkDim = Color(0xFF868F98);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFD9736A);
}
