import 'package:flutter/material.dart';

/// The sham's colours: a drum ring at night, hits in red skin, rests in
/// dim brass, the ring in bronze, gaps told in chalk.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const ring = Color(0xFF6B5A33);
  static const rest = Color(0xFF3E3A46);
  static const restRim = Color(0xFF6B6570);
  static const hit = Color(0xFFC94F44);
  static const hitRim = Color(0xFFF0A08A);
  static const gapArc = Color(0xFFD1A85E);
  static const gapArcOdd = Color(0xFF8C6E3E);
  static const chalk = Color(0xFFECEAE2);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
