import 'package:flutter/material.dart';

/// The sham's colours: a drawing board at night, the square in chalk,
/// the diagonal in red, the ladder in brass and the true diagonal in
/// gold.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const chalk = Color(0xFFB9BCC4);
  static const chalkFace = Color(0xFF2A2C34);
  static const diagonal = Color(0xFFE0645A);
  static const rung = Color(0xFFD1A85E);
  static const rungDim = Color(0xFF6B5A33);
  static const truth = Color(0xFFE8C46A);
  static const rule = Color(0xFF4A4650);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
