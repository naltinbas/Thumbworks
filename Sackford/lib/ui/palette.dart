import 'package:flutter/material.dart';

/// The sham's colours: the carrier's yard at dusk, hessian sacks, carts
/// with a load bar to the ten-stone line.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const yard = Color(0xFF26222B);
  static const ground = Color(0xFF3A3129);
  static const sack = Color(0xFFC9A468);
  static const sackDark = Color(0xFF8C6A3A);
  static const cart = Color(0xFF5B3A1E);
  static const cartBed = Color(0xFF3F3A47);
  static const loadOk = Color(0xFF81C784);
  static const loadOver = Color(0xFFE57373);
  static const capLine = Color(0xFFECEAE2);
  static const carts = [
    Color(0xFF9CC5E8),
    Color(0xFFA9D3A0),
    Color(0xFFCDB4E5),
    Color(0xFFF0A9A0),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
