import 'package:flutter/material.dart';

/// The colours.
///
/// A shop counter by lamplight: dark wood, a brass till, copper and silver
/// coins. The copper is for the pennies, the silver for everything from the
/// sixpence up, the way the coins really were.
class Palette {
  const Palette._();

  static const night = Color(0xFF120F0C);
  static const verge = Color(0xFF1D1813);
  static const line = Color(0xFF2C241B);
  static const edge = Color(0xFF463A2B);

  static const brass = Color(0xFF9A7E46);
  static const copper = Color(0xFFA9714B);
  static const copperRim = Color(0xFF7E5335);
  static const silver = Color(0xFFB9BEC2);
  static const silverRim = Color(0xFF8A9095);

  static const ink = Color(0xFFEFE8DB);
  static const inkDim = Color(0xFF95897A);
  static const good = Color(0xFF9CBB6D);
  static const bad = Color(0xFFD9736A);
}
