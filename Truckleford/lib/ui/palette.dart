import 'package:flutter/material.dart';

/// The sham's colours: a yard at dusk, wagons in rust, the one that can
/// move next lit by the lamp.
class Palette {
  static const night = Color(0xFF14161A);
  static const board = Color(0xFF1E222A);

  static const ink = Color(0xFFE7E9EC);
  static const inkDim = Color(0xFF8E949C);

  static const line = Color(0xFF39404B);
  static const rail = Color(0xFF6E7681);
  static const wagon = Color(0xFFB0653A);
  static const sided = Color(0xFF4E6E8C);
  static const sent = Color(0xFF5E8C61);
  static const lamp = Color(0xFFE0A33C);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
