import 'package:flutter/material.dart';

/// The sham's colours: a warm table at night, wives in plum,
/// husbands in slate, rust where a couple sits together.
class Palette {
  static const night = Color(0xFF161216);
  static const board = Color(0xFF231D23);

  static const ink = Color(0xFFEEE7EC);
  static const inkDim = Color(0xFFA79CA4);

  static const line = Color(0xFF453745);
  static const table = Color(0xFF2A222A);
  static const tableRim = Color(0xFF6E5866);
  static const wife = Color(0xFFB4699C);
  static const husband = Color(0xFF5E7A99);
  static const chair = Color(0xFF4A3B48);
  static const held = Color(0xFF8A6B42);

  static const quarrel = Color(0xFFE57373);
  static const parted = Color(0xFF81C784);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
