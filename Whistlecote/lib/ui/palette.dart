import 'package:flutter/material.dart';

/// The sham's colours: a moor at dusk, the whistle tree in stone, the
/// calls each a colour of their own, rust where one whistle starts
/// another and green where every call has its notes.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const moor = Color(0xFF2B2430);
  static const heather = Color(0xFF3D2E42);
  static const grass = Color(0xFF2F3A2B);
  static const stone = Color(0xFF6E6A66);
  static const stoneDim = Color(0xFF3F3C42);
  static const branch = Color(0xFF7A736C);
  static const gold = Color(0xFFD9A441);
  static const clash = Color(0xFFE57373);
  static const whole = Color(0xFF4A4452);

  /// One colour to a call, in the order the calls are given.
  static const calls = [
    Color(0xFFD9A441),
    Color(0xFF64B5F6),
    Color(0xFF9575CD),
    Color(0xFF4DB6AC),
    Color(0xFFE0C097),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
