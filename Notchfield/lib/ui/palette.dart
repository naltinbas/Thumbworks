import 'package:flutter/material.dart';

/// The colours of the game: brass rule on workshop slate.
class Palette {
  static const slate = Color(0xFF14171A);
  static const panel = Color(0xFF1F2328);
  static const line = Color(0xFF363C44);
  static const edge = Color(0xFF525A66);
  static const ink = Color(0xFFEDEBE3);
  static const inkDim = Color(0xFF9AA3AE);

  static const rule = Color(0xFF2A2F36);
  static const ruleEdge = Color(0xFF4A5361);
  static const notch = Color(0xFFC9A25B);
  static const notchEdge = Color(0xFF8F7239);

  /// The census: a length measured once, unmeasured, or twice over.
  static const once = Color(0xFF8FBD8F);
  static const unmeasured = Color(0xFF46525C);
  static const twice = Color(0xFFD9705C);

  static const shown = Color(0xFF64B6F0);

  static const good = Color(0xFF8FBD8F);
  static const bad = Color(0xFFD9705C);
}
