import 'package:flutter/material.dart';

/// The worth's colours: lit windows on a night house, the walk
/// written beneath, darkness in gold when it lands.
class Palette {
  static const night = Color(0xFF121318);
  static const board = Color(0xFF1D1F26);

  static const ink = Color(0xFFEAECF1);
  static const inkDim = Color(0xFF9DA2B0);

  static const line = Color(0xFF383B47);
  static const frame = Color(0xFF565B6E);
  static const glow = Color(0xFFE8C96A);
  static const dim = Color(0xFF2A2C36);

  static const rest = Color(0xFFE0BE52);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
