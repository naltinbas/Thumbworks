import 'package:flutter/material.dart';

/// The sham's colours: a small board in walnut and maple, bishops in
/// ivory, a clash struck in rust along its diagonal.
class Palette {
  static const night = Color(0xFF16151A);
  static const board = Color(0xFF211F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3A3846);
  static const frame = Color(0xFF4A3119);
  static const light = Color(0xFFE6D3AE);
  static const dark = Color(0xFF8B5E3C);
  static const mitre = Color(0xFFF6EFE0);
  static const mitreEdge = Color(0xFF6E5A46);
  static const held = Color(0xFFD9A441);
  static const clash = Color(0xFFD9534F);
  static const peace = Color(0xFF81C784);
  static const lonely = Color(0xFF3F6E8C);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
