import 'package:flutter/material.dart';

/// The colours of the game: a parish hall by lamplight.
class Palette {
  static const hall = Color(0xFF171318);
  static const panel = Color(0xFF221C24);
  static const line = Color(0xFF3B323E);
  static const edge = Color(0xFF564A5A);
  static const ink = Color(0xFFEFE9E2);
  static const inkDim = Color(0xFFA298A6);

  static const chip = Color(0xFF2E2631);
  static const chipRim = Color(0xFF4A3F4E);
  static const face = Color(0xFFE8C89A);

  /// The cords: a standing couple, an eloping pair, a pointed one, and
  /// the armed rim.
  static const cord = Color(0xFF8FB98F);
  static const elope = Color(0xFFD9705C);
  static const shown = Color(0xFF64B6F0);
  static const armed = Color(0xFF64B6F0);

  static const good = Color(0xFF8FB98F);
  static const bad = Color(0xFFD9705C);
}
