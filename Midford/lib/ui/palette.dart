import 'package:flutter/material.dart';

/// The sham's colours: a pegboard in dark wood, pegs in brass, the
/// cords in cream, the midpoint figure in gold, the diagonals faint.
class Palette {
  static const night = Color(0xFF14110E);
  static const board = Color(0xFF211C16);

  static const ink = Color(0xFFF0EAE0);
  static const inkDim = Color(0xFFA89F91);

  static const line = Color(0xFF3F3729);
  static const wood = Color(0xFF2E2620);
  static const hole = Color(0xFF1B1611);
  static const peg = Color(0xFFD9A441);
  static const pegGiven = Color(0xFF8E7A4A);
  static const pegInk = Color(0xFF2B2108);
  static const cord = Color(0xFFE6DCC4);
  static const midpoint = Color(0xFFE0BC7A);
  static const figure = Color(0xFFD9B44A);
  static const diagonal = Color(0xFF7FA0A8);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
