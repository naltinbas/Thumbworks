import 'package:flutter/material.dart';

/// The sham's colours: a lamplit floor, dancers in ochre, pairs
/// that come to one in green thread, pairs that do not in rust,
/// the two who keep to themselves in dim gold.
class Palette {
  static const night = Color(0xFF17130E);
  static const board = Color(0xFF241E16);

  static const ink = Color(0xFFF0E9DC);
  static const inkDim = Color(0xFFA89E8C);

  static const line = Color(0xFF443A2C);
  static const floor = Color(0xFF2A2219);
  static const floorRim = Color(0xFF6A5A42);
  static const dancer = Color(0xFFC98D4B);
  static const dancerInk = Color(0xFF1C140A);
  static const alone = Color(0xFF8A7A52);

  static const sound = Color(0xFF81C784);
  static const sour = Color(0xFFE57373);

  static const picked = Color(0xFFFFFFFF);
  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
