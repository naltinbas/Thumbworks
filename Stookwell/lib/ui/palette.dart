import 'package:flutter/material.dart';

/// The sham's colours: a stubble field at harvest, sheaves in straw
/// gold, stooks stood in rows, the pool lying flat below.
class Palette {
  static const night = Color(0xFF17161A);
  static const board = Color(0xFF232128);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const stubble = Color(0xFF6E5A2E);
  static const stubbleDim = Color(0xFF5E4C26);
  static const rowLine = Color(0xFF7E6A3A);
  static const straw = Color(0xFFE8C45C);
  static const strawDark = Color(0xFFB88E2E);
  static const band = Color(0xFF8A5A2B);
  static const ear = Color(0xFFF4DA8C);
  static const clash = Color(0xFFE57373);
  static const fine = Color(0xFF81C784);
  static const pool = Color(0xFF3F3520);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
