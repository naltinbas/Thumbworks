import 'package:flutter/material.dart';

/// The sham's colours: a market scale in iron and brass, the load in
/// sackcloth, the weights brass, the beam level in green and tipped
/// in rust.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF1F2127);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF383D45);
  static const iron = Color(0xFF4A4E58);
  static const ironDark = Color(0xFF2E3138);
  static const brass = Color(0xFFD9A441);
  static const brassDark = Color(0xFF8A6A1F);
  static const brassInk = Color(0xFF3B2A08);
  static const sack = Color(0xFFB08A5A);
  static const sackDark = Color(0xFF7A5C34);
  static const pan = Color(0xFF6E7684);
  static const level = Color(0xFF81C784);
  static const tipped = Color(0xFFE57373);
  static const ground = Color(0xFF2A2620);
  static const barred = Color(0xFF3A3A44);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
