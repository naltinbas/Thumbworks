import 'package:flutter/material.dart';

/// The sham's colours: an orchard at night, the trees in sight in leaf
/// green, the hidden ones dim, the line of sight in gold, the tree in
/// the way ringed red.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const grass = Color(0xFF1B211C);
  static const leaf = Color(0xFF7DB77A);
  static const leafDim = Color(0xFF3F5240);
  static const trunk = Color(0xFF8A6A48);
  static const chalk = Color(0xFFD9D4C4);
  static const gold = Color(0xFFE8C46A);
  static const gate = Color(0xFFF2E6B8);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
