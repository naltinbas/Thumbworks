import 'dart:ui';

/// A horse yard in the afternoon.
class Palette {
  const Palette._();

  /// Beyond the fence.
  static const beyond = Color(0xFF191D17);

  /// Panels and cards: the stable boards.
  static const stable = Color(0xFF262B22);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3B4234);

  /// Borders that mean something.
  static const edge = Color(0xFF636E55);

  /// Words.
  static const ink = Color(0xFFEAEBDD);

  /// Words standing back.
  static const inkDim = Color(0xFF99A18C);

  /// The two grasses: the colour argument lives here.
  static const dark = Color(0xFF2C3A28);
  static const light = Color(0xFF43523B);

  /// The fence round the yard.
  static const fence = Color(0xFF6E6249);

  /// The colt, and his path and hoofprints.
  static const colt = Color(0xFFB4744A);
  static const hoof = Color(0xFF87684C);
  static const path = Color(0xFFC9A968);

  /// The gate the round begins at.
  static const gate = Color(0xFFD9BE55);

  /// The pointed-at paddock when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A round ridden, and one stranded.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
