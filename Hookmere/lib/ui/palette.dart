import 'package:flutter/material.dart';

/// The sham's colours: a joiner's bench, boxes in pale timber, the one
/// in hand lit, the hooks written in pencil.
class Palette {
  static const night = Color(0xFF16150F);
  static const board = Color(0xFF211F16);

  static const ink = Color(0xFFEEE9DA);
  static const inkDim = Color(0xFF988F7C);

  static const line = Color(0xFF423A29);
  static const box = Color(0xFFBFA97C);
  static const held = Color(0xFFF0DFAE);
  static const corner = Color(0xFFD9B25A);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
