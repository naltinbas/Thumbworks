import 'package:flutter/material.dart';

/// The sham's colours: a moor of holes tinted faintly by kind, pegs
/// in brass, halfway posts in green where they land and rust where
/// they fall between.
class Palette {
  static const night = Color(0xFF12140F);
  static const board = Color(0xFF1C2117);

  static const ink = Color(0xFFEDEFE6);
  static const inkDim = Color(0xFF9EA491);

  static const line = Color(0xFF373D2C);
  static const moor = Color(0xFF232A1E);
  static const peg = Color(0xFFD9A441);
  static const pegInk = Color(0xFF2B2108);
  static const cord = Color(0xFF7FA0A8);
  static const landed = Color(0xFF81C784);
  static const between = Color(0xFFE57373);

  /// The four kinds of hole, faint.
  static const kinds = [
    Color(0xFF3B4531),
    Color(0xFF45392E),
    Color(0xFF2E3F45),
    Color(0xFF443040),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
