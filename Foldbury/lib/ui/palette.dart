import 'package:flutter/material.dart';

/// The colours.
///
/// A sheep fold on the downs at dusk: pale gates, dark turf, and the lanes
/// going from shadow to lamplight as the shepherds take their posts.
class Palette {
  const Palette._();

  static const night = Color(0xFF0E110E);
  static const verge = Color(0xFF171C17);
  static const line = Color(0xFF242B24);
  static const edge = Color(0xFF3B453B);

  static const dark = Color(0xFF37413B);
  static const watched = Color(0xFFC9BC8F);
  static const gate = Color(0xFF6E7A6E);
  static const shepherd = Color(0xFFD8A54F);
  static const pair = Color(0xFF6FA8CF);

  static const ink = Color(0xFFEBEEE9);
  static const inkDim = Color(0xFF889086);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
