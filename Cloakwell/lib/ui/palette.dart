import 'package:flutter/material.dart';

/// The sham's colours: a cloakroom rail in oak with brass hooks, coats
/// in cloth, pairs out of order strung above in rust and the sorted
/// row in green.
class Palette {
  static const night = Color(0xFF16151A);
  static const board = Color(0xFF211F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const wall = Color(0xFF2E2A33);
  static const rail = Color(0xFF6E4B2A);
  static const railEdge = Color(0xFF4A3119);
  static const brass = Color(0xFFD9A441);
  static const askew = Color(0xFFE57373);
  static const inOrder = Color(0xFF81C784);
  static const gap = Color(0xFF3B3947);

  /// The coats' cloth, by number.
  static const cloth = [
    Color(0xFF8B3A3A),
    Color(0xFF2F5D8A),
    Color(0xFF3F7A4A),
    Color(0xFF7A5A9A),
    Color(0xFFB0762E),
    Color(0xFF5FA8A0),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
