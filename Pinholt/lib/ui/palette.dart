import 'package:flutter/material.dart';

/// The sham's colours: a cork board at dusk, brass pins, the
/// fence in gold, frames in green, a refused hole in rust.
class Palette {
  static const night = Color(0xFF14110D);
  static const board = Color(0xFF211B15);

  static const ink = Color(0xFFEFE8DD);
  static const inkDim = Color(0xFFA79C8C);

  static const line = Color(0xFF3F3629);
  static const cork = Color(0xFF3A2E22);
  static const corkRim = Color(0xFF6A5843);
  static const hole = Color(0xFF241C14);
  static const pin = Color(0xFFD9A441);
  static const pinInk = Color(0xFF3A2A0C);

  static const fence = Color(0xFFE0B860);
  static const frame = Color(0xFF81C784);
  static const refused = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
