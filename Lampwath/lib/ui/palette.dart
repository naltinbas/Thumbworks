import 'package:flutter/material.dart';

/// The colours.
///
/// A stone bridge at night: black water, the parapet just visible, and one
/// warm lantern that is the only light anybody has.
class Palette {
  const Palette._();

  static const night = Color(0xFF0B0E13);
  static const verge = Color(0xFF151A21);
  static const line = Color(0xFF212833);
  static const edge = Color(0xFF39424E);

  static const stone = Color(0xFF4A4E55);
  static const water = Color(0xFF10151E);
  static const walker = Color(0xFF8A94A0);
  static const chosen = Color(0xFFE0B15C);
  static const lantern = Color(0xFFF0C468);

  static const ink = Color(0xFFEAEDF1);
  static const inkDim = Color(0xFF89929D);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
