import 'package:flutter/material.dart';

/// The sham's colours: a courtyard of grey flags by night, the flags a
/// watchman sees lit amber, the flags no one sees left dark, the far
/// flags chalked on the hopeless yard, green when every flag is watched.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const wall = Color(0xFF2E2A2A);
  static const dark = Color(0xFF3A3838);
  static const grout = Color(0xFF262424);
  static const lit = Color(0xFF8A6A3A);
  static const litBright = Color(0xFFB08A48);
  static const lantern = Color(0xFFFFD166);
  static const coat = Color(0xFF2A2C3A);
  static const chalk = Color(0xFFECEAE2);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
