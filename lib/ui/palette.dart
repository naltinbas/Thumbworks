import 'package:flutter/material.dart';

/// The colours.
///
/// A yard at dusk: floor, walls, and three things that move or matter — the
/// marks, the crates, and whoever is shoving them.
class Palette {
  const Palette._();

  static const night = Color(0xFF15181C);
  static const shed = Color(0xFF1E2228);

  static const floor = Color(0xFF262C33);
  static const wall = Color(0xFF39414B);
  static const wallTop = Color(0xFF454E59);

  static const mark = Color(0xFFD08A45);
  static const crate = Color(0xFFC08A4B);
  static const crateEdge = Color(0xFF9A6B36);
  static const crateHome = Color(0xFF7DA85E);
  static const crateHomeEdge = Color(0xFF5F8546);

  static const hauler = Color(0xFF6FB3D2);

  static const ink = Color(0xFFE6E9EC);
  static const inkDim = Color(0xFF8A939C);
  static const warn = Color(0xFFD9645A);
  static const good = Color(0xFF7DA85E);
}
