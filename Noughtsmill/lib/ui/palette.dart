import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF181818);
  static const board = Color(0xFF232322);
  static const line = Color(0xFF373735);

  /// The mill and its stones.
  static const mill = Color(0xFF6E5A44);
  static const stone = Color(0xFF8A8578);

  /// The noughts on the end, and the ledger.
  static const nought = Color(0xFFE8B84B);
  static const ledgerInk = Color(0xFF5BB98C);

  /// The digit face of the winding.
  static const face = Color(0xFFE8E2D6);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
