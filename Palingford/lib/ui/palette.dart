import 'package:flutter/material.dart';

/// A fence at dusk: timber palings against a dark field, the climb marked in
/// green and the drop in rust, and the one in hand lit paler than the rest.
class Palette {
  static const night = Color(0xFF12140F);
  static const board = Color(0xFF1B1E17);

  static const ink = Color(0xFFE8E5D6);
  static const inkDim = Color(0xFF908B76);

  static const line = Color(0xFF383A2C);
  static const ground = Color(0xFF4E5140);
  static const paling = Color(0xFF9A8259);
  static const held = Color(0xFFDCC489);

  static const climb = Color(0xFF89AD6C);
  static const drop = Color(0xFFC26B4C);

  static const shown = Color(0xFF6BA5D4);
  static const good = Color(0xFF89AD6C);
  static const bad = Color(0xFFC26B4C);
}
