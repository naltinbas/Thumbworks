import 'package:flutter/material.dart';

/// The sham's colours: a square of dots on slate, the inner square dim,
/// the run's bands in copper and gold turn about, the sums in chalk.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const innerDot = Color(0xFF3A3A48);
  static const bandA = Color(0xFFC98E5A);
  static const bandB = Color(0xFFE8C46A);
  static const outline = Color(0xFF55556A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
