import 'package:flutter/material.dart';

/// The sham's colours: a carpenter's yard at night, the gable in oak,
/// the height in chalk, a whole area washed green, the sides told in
/// brass.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const oak = Color(0xFFB8874F);
  static const oakFace = Color(0xFF3A2E22);
  static const wholeFace = Color(0xFF29402E);
  static const chalk = Color(0xFFB9BCC4);
  static const brass = Color(0xFFD1A85E);
  static const flat = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
