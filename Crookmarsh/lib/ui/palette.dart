import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF171A19);
  static const board = Color(0xFF212524);
  static const line = Color(0xFF323836);

  /// The marsh and its crossings.
  static const reed = Color(0xFF2A3330);
  static const cross = Color(0xFF48564F);

  /// The posts.
  static const post = Color(0xFFB8B2A4);
  static const postRim = Color(0xFF6E6A5E);

  /// A true frame, washed and ringed.
  static const frame = Color(0xFF5BB98C);

  /// Three posts sharing a line, called out.
  static const lined = Color(0xFFC96A4A);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
