import 'package:flutter/material.dart';

/// The sham's colours: a sheet of paper on the desk, M in blue, I in
/// gold, U in plum, the target in outline above, the derived string in
/// green and the spent one in rust.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const desk = Color(0xFF2E2A33);
  static const sheet = Color(0xFFEDE6D6);
  static const pen = Color(0xFF2A2420);
  static const faint = Color(0xFF9A9280);
  static const tileM = Color(0xFF5B9BD5);
  static const tileI = Color(0xFFD9A441);
  static const tileU = Color(0xFF9575CD);
  static const mark = Color(0xFF3F8A4A);
  static const clash = Color(0xFFB0523C);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
