import 'package:flutter/material.dart';

/// The holme's colours: star lanes on a deep night, the walk
/// in lantern gold, the round closing green.
class Palette {
  static const night = Color(0xFF10121A);
  static const board = Color(0xFF1B1E2A);

  static const ink = Color(0xFFE9EAF2);
  static const inkDim = Color(0xFF9BA0B4);

  static const lane = Color(0xFF3A3F55);
  static const post = Color(0xFF6E7590);
  static const walked = Color(0xFFE0BE52);
  static const head = Color(0xFFF0D98C);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
