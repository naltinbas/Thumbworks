import 'package:flutter/material.dart';

/// The sham's colours: a counting house by lamplight, brass wheels on
/// ledger paper gone dark with age.
class Palette {
  static const night = Color(0xFF14161B);
  static const board = Color(0xFF1D2029);

  static const ink = Color(0xFFE9EAEF);
  static const inkDim = Color(0xFF8F94A2);

  static const line = Color(0xFF373C4A);
  static const drum = Color(0xFF4A5265);
  static const brass = Color(0xFFC9A24B);
  static const lit = Color(0xFFF0D283);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
