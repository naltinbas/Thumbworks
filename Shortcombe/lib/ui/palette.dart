import 'package:flutter/material.dart';

/// The sham's colours: four junctions on a night map, the fixed roads
/// slate, the variable roads copper widening with the crowd, the
/// shortcut gold when open, and the drivers' minutes in chalk.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const junction = Color(0xFFD9D4C4);
  static const fixedRoad = Color(0xFF55556A);
  static const road = Color(0xFFC98E5A);
  static const roadDim = Color(0xFF4A3A2A);
  static const shortcut = Color(0xFFE8C46A);
  static const shortcutShut = Color(0xFF3A3A48);
  static const faster = Color(0xFF81C784);
  static const slower = Color(0xFFD9695A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
