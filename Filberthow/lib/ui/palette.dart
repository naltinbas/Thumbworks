import 'dart:ui';

/// A beech wood in October.
class Palette {
  const Palette._();

  /// The wood behind everything.
  static const wood = Color(0xFF1C1913);

  /// Panels and cards: the log boards.
  static const log = Color(0xFF2A2418);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF44392A);

  /// Borders that mean something.
  static const edge = Color(0xFF6E5C40);

  /// Words.
  static const ink = Color(0xFFF0E9D6);

  /// Words standing back.
  static const inkDim = Color(0xFFA4937A);

  /// A hazelnut: shell, sheen, and the pale scar at its base.
  static const shell = Color(0xFFB07A44);
  static const sheen = Color(0xFFCB9A5E);
  static const scar = Color(0xFFE3D3B3);

  /// A nut marked for taking, and one the grey squirrel took.
  static const marked = Color(0xFF8FB8D9);
  static const theirs = Color(0xFF8A8474);

  /// The cluster rings, when the game is asked why.
  static const cluster = Color(0xFFD9BE55);

  /// A hoard won, and one lost.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
