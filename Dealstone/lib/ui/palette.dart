import 'package:flutter/material.dart';

/// The stone's colours: river stones stacked on dark turf, the
/// stair arriving in gold.
class Palette {
  static const night = Color(0xFF131511);
  static const board = Color(0xFF1F231C);

  static const ink = Color(0xFFECEDE2);
  static const inkDim = Color(0xFFA0A691);

  static const line = Color(0xFF3B4232);
  static const slotRim = Color(0xFF4A5340);
  static const stone = Color(0xFF9BA48E);
  static const stoneRim = Color(0xFF6E7562);
  static const pool = Color(0xFF77816B);

  static const stair = Color(0xFFE0BE52);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
