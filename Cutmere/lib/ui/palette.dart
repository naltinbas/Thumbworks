import 'package:flutter/material.dart';

/// The sham's colours: a cellar of stone, casks in oak with iron hoops,
/// the casks ruled out gone dark, the coin in gold, the found cask in
/// green and the spent search in rust.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const cellar = Color(0xFF2A2622);
  static const stone = Color(0xFF3A3530);
  static const oak = Color(0xFFA9773F);
  static const oakDark = Color(0xFF6B4A24);
  static const hoop = Color(0xFF55585E);
  static const out = Color(0xFF3F3833);
  static const gold = Color(0xFFD9A441);
  static const chalk = Color(0xFFECEAE2);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
