import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF141A1F);
  static const board = Color(0xFF1C242B);
  static const line = Color(0xFF2C363F);

  /// The hoop's wood and its holes.
  static const hoop = Color(0xFF4A3F33);
  static const hole = Color(0xFF6B5D4A);

  /// The pegs.
  static const peg = Color(0xFFE8B84B);
  static const pegRim = Color(0xFF8A6D2B);

  /// The three gap lengths, shortest to longest.
  static const gapOne = Color(0xFF5BB98C);
  static const gapTwo = Color(0xFF5B9BD5);
  static const gapThree = Color(0xFFC96A4A);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
