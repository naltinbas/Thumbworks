import 'package:flutter/material.dart';

/// A dark room with one mirror: the glass cold, the light warm, and the
/// folded half of the picture drawn faint below the glass.
class Palette {
  static const night = Color(0xFF0E1014);
  static const board = Color(0xFF171A20);

  static const ink = Color(0xFFE9EBF0);
  static const inkDim = Color(0xFF8A919E);

  static const line = Color(0xFF2E343E);
  static const glass = Color(0xFF6E7A8C);
  static const peg = Color(0xFF4A5261);

  static const beam = Color(0xFFE7C25C);
  static const lamp = Color(0xFFF0D98A);
  static const eye = Color(0xFF7FC2A8);
  static const fold = Color(0xFF5C6473);

  static const shown = Color(0xFF6FA8D6);
  static const good = Color(0xFF7FBF72);
  static const bad = Color(0xFFD2705C);
}
