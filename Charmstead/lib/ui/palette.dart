import 'package:flutter/material.dart';

/// The colours of the game: brass coins on a hearthstone bed.
class Palette {
  static const hearth = Color(0xFF161311);
  static const panel = Color(0xFF211D19);
  static const line = Color(0xFF3C352D);
  static const edge = Color(0xFF5A5044);
  static const ink = Color(0xFFF0EBE1);
  static const inkDim = Color(0xFFA79E90);

  static const bed = Color(0xFF1C1916);
  static const coin = Color(0xFFC9A25B);
  static const coinEdge = Color(0xFF8F7239);
  static const coinInk = Color(0xFF2E2410);
  static const pinStud = Color(0xFF6E5A42);

  /// The rims and sums: an armed coin, a pointed mend, kept and
  /// broken lines.
  static const armed = Color(0xFF64B6F0);
  static const shown = Color(0xFF64B6F0);
  static const kept = Color(0xFF8FBD8F);
  static const brokenLine = Color(0xFFD9705C);

  static const good = Color(0xFF8FBD8F);
  static const bad = Color(0xFFD9705C);
}
