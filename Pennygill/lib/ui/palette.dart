import 'dart:ui';

/// A tavern table by candlelight.
class Palette {
  const Palette._();

  /// The room behind everything.
  static const taproom = Color(0xFF1D1A15);

  /// Panels and cards: the table boards.
  static const board = Color(0xFF2A251D);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF43392B);

  /// Borders that mean something.
  static const edge = Color(0xFF6E5D42);

  /// Words.
  static const ink = Color(0xFFEFE8D6);

  /// Words standing back.
  static const inkDim = Color(0xFFA3947A);

  /// A head, a tail, and the coin's rim.
  static const head = Color(0xFFE3B94F);
  static const tail = Color(0xFF8A8474);
  static const rim = Color(0xFF54492F);

  /// Your side and the house's.
  static const you = Color(0xFF63B0E3);
  static const house = Color(0xFFC98F4E);

  /// The ring of calls, when the game is asked why.
  static const ring = Color(0xFFD9BE55);

  /// The pointed-at call when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A match taken, and one lost.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
