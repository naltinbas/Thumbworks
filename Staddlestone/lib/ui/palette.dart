import 'package:flutter/material.dart';

/// The colours.
///
/// A rickyard at evening: staddle stones pale under their caps, millstones
/// the grey of gritstone with a warm one lifted, grass nearly black.
class Palette {
  const Palette._();

  static const night = Color(0xFF0E110D);
  static const verge = Color(0xFF171B16);
  static const line = Color(0xFF242A22);
  static const edge = Color(0xFF3D453A);

  static const staddle = Color(0xFF8D8578);
  static const stone = Color(0xFF6E6E72);
  static const stoneEdge = Color(0xFF4E4E52);
  static const liftedStone = Color(0xFFC8A25C);
  static const home = Color(0xFF7B8F72);

  static const ink = Color(0xFFECEEE9);
  static const inkDim = Color(0xFF8B9086);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
