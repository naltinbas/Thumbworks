import 'dart:ui';

/// A mercer's bench by lamplight.
class Palette {
  const Palette._();

  /// The shop behind everything.
  static const shop = Color(0xFF1B1817);

  /// Panels and cards: the bench boards.
  static const bench = Color(0xFF292321);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF423834);

  /// Borders that mean something.
  static const edge = Color(0xFF6E5B50);

  /// Words.
  static const ink = Color(0xFFF0E9E0);

  /// Words standing back.
  static const inkDim = Color(0xFFA3948A);

  /// The long bolt, the short bolt, and their selvedges.
  static const longCloth = Color(0xFF7D4A52);
  static const longSelvedge = Color(0xFF9C6670);
  static const shortCloth = Color(0xFF4A6E5C);
  static const shortSelvedge = Color(0xFF66907C);

  /// A cut marked for taking, and the golden tick.
  static const marked = Color(0xFF8FB8D9);
  static const golden = Color(0xFFE0B34E);

  /// A bench held, and one lost.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
