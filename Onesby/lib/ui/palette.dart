import 'package:flutter/material.dart';

/// The sham's colours: a row of ones on slate, prime rows in gold,
/// composite in rust, the chain in blue.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const cell = Color(0xFF2A2A36);
  static const one = Color(0xFFE8C46A);
  static const oneRust = Color(0xFFD9695A);
  static const chain = Color(0xFF6FA8DC);
  static const floor = Color(0xFF3A3A48);
  static const number = Color(0xFFF2E6B8);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
