import 'package:flutter/material.dart';

/// A hall at supper: the trestles scrubbed pale, the guests in warm
/// wool, and a table nobody sits at drawn as bare boards.
class Palette {
  static const night = Color(0xFF14120F);
  static const board = Color(0xFF1E1B16);

  static const ink = Color(0xFFEDE8DE);
  static const inkDim = Color(0xFF988F80);

  static const line = Color(0xFF3A352C);
  static const trestle = Color(0xFF6B5F4A);
  static const bare = Color(0xFF2A251E);

  static const guest = Color(0xFFD9C08A);
  static const alone = Color(0xFFC97D53);
  static const paired = Color(0xFF8DB073);

  static const shown = Color(0xFF6FA8D6);
  static const good = Color(0xFF8DB073);
  static const bad = Color(0xFFC97D53);
}
