import 'package:flutter/material.dart';

/// The colours.
///
/// Water on stone. The pipes are cut channels, what is running through them is
/// the one bright thing on the screen, and the only red is a pipe that is as
/// full as it will ever be.
class Palette {
  const Palette._();

  static const night = Color(0xFF101416);
  static const stone = Color(0xFF1A2124);
  static const cut = Color(0xFF252E32);
  static const edge = Color(0xFF39464B);

  static const water = Color(0xFF56B8D8);
  static const waterEdge = Color(0xFF9FE0F2);
  static const full = Color(0xFFE0A05F);
  static const spring = Color(0xFF7FC98E);
  static const mill = Color(0xFFC98E7F);

  static const ink = Color(0xFFE9EEF0);
  static const inkDim = Color(0xFF8A9599);
  static const good = Color(0xFF7FC98E);
  static const bad = Color(0xFFD4736B);
}
