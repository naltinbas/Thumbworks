import 'package:flutter/material.dart';

/// The sham's colours: a school slate for the week, the girls in nine
/// tints, given days in chalk and built days in gold, pairs met ticked
/// green and repeated rust.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF1F2127);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF383D45);
  static const slate = Color(0xFF2B3432);
  static const slateLine = Color(0xFF3F4A47);
  static const chalk = Color(0xFFE9E4D4);
  static const chalkDim = Color(0xFF7E8A86);
  static const gold = Color(0xFFD9A441);
  static const bench = Color(0xFF3A3226);
  static const met = Color(0xFF81C784);
  static const twice = Color(0xFFE57373);
  static const unmet = Color(0xFF3F4A47);

  /// The girls' tints, A to I.
  static const girls = [
    Color(0xFF6C86B0),
    Color(0xFFB0523C),
    Color(0xFF7BA05B),
    Color(0xFFC49A3C),
    Color(0xFF9C6BAF),
    Color(0xFF5FA8A0),
    Color(0xFFC46B8A),
    Color(0xFF8A8A5A),
    Color(0xFF5A7DA0),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
