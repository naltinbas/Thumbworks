import 'package:flutter/material.dart';

/// The sham's colours: a decimal on slate, the repeating block in gold
/// under its bar, the remainders' ring in copper.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const face = Color(0xFF3A3A48);
  static const hourDim = Color(0xFF4A4A5A);
  static const digit = Color(0xFFE8C46A);
  static const bar = Color(0xFFF2D77A);
  static const walk = Color(0xFFC98E5A);
  static const start = Color(0xFFF2E6B8);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
