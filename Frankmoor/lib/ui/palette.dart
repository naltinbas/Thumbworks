import 'dart:ui';

/// A village post office counter.
class Palette {
  const Palette._();

  /// The office behind everything.
  static const office = Color(0xFF1B1917);

  /// Panels and cards: the counter boards.
  static const counter = Color(0xFF292521);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF423B33);

  /// Borders that mean something.
  static const edge = Color(0xFF6E6152);

  /// Words.
  static const ink = Color(0xFFF0EAE0);

  /// Words standing back.
  static const inkDim = Color(0xFFA3988A);

  /// The envelope, and its written address lines.
  static const envelope = Color(0xFFD9CFBB);
  static const address = Color(0xFF8A8072);

  /// The cheap stamp, and the dear one.
  static const cheap = Color(0xFF5E8C66);
  static const cheapDeep = Color(0xFF476B4E);
  static const dear = Color(0xFFB05A52);
  static const dearDeep = Color(0xFF8A443E);

  /// The walk chips, when the game is asked why.
  static const chip = Color(0xFFD9BE55);
  static const chipHit = Color(0xFF8FBF7F);

  /// A letter franked, and one past paying.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
