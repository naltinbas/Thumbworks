import 'dart:ui';

/// A griddle at the fair, before the crowd comes.
class Palette {
  const Palette._();

  /// The room behind everything.
  static const iron = Color(0xFF201C16);

  /// Panels and cards: the hearth boards.
  static const hearth = Color(0xFF2C261E);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF453B2D);

  /// Borders that mean something.
  static const edge = Color(0xFF6E5C41);

  /// Words.
  static const ink = Color(0xFFEFE6D2);

  /// Words standing back.
  static const inkDim = Color(0xFFA19173);

  /// A cake, its crust, and its shaded underside.
  static const cake = Color(0xFFDDA85C);
  static const crust = Color(0xFFA87838);
  static const shade = Color(0xFFC08F49);

  /// The size baked into the middle of a cake.
  static const brand = Color(0xFF56391C);

  /// The griddle plate the batch sits on.
  static const plate = Color(0xFF141210);
  static const plateRim = Color(0xFF4A4238);

  /// A gap between neighbours that are not next in size.
  static const gap = Color(0xFFD96C4E);

  /// The slice, when the game is asked to show.
  static const slice = Color(0xFF8FB8D9);

  /// A batch at the fewest, and one gone over.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
