import 'package:flutter/material.dart';

/// The sham's colours: a baker's board, the loaf in crust and
/// crumb, cuts in warm tints, the share's mark in gold.
class Palette {
  static const night = Color(0xFF15120E);
  static const board = Color(0xFF221D17);

  static const ink = Color(0xFFF0E9DC);
  static const inkDim = Color(0xFFA89E8C);

  static const line = Color(0xFF41382C);
  static const crust = Color(0xFF8A5A2B);
  static const crumb = Color(0xFF3A2F22);
  static const shareMark = Color(0xFFD9B44A);
  static const tile = Color(0xFF2E2620);
  static const tileTaken = Color(0xFF6E4E2C);
  static const tileInk = Color(0xFFEFE6D6);
  static const over = Color(0xFFE57373);

  /// The cuts, tinted in turn.
  static const cuts = [
    Color(0xFFD9A05B),
    Color(0xFFC77D4A),
    Color(0xFFE0BC7A),
    Color(0xFFB8874F),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
