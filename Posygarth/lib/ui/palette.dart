import 'dart:ui';

/// A walled show garden in June.
class Palette {
  const Palette._();

  /// Beyond the wall.
  static const dusk = Color(0xFF191B16);

  /// Panels and cards: the potting bench.
  static const bench = Color(0xFF262A21);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3C4234);

  /// Borders that mean something.
  static const edge = Color(0xFF606B54);

  /// Words.
  static const ink = Color(0xFFEDEFE4);

  /// Words standing back.
  static const inkDim = Color(0xFF9BA48F);

  /// The bed soil, two tones.
  static const soil = Color(0xFF2C2A20);
  static const soilLight = Color(0xFF353327);

  /// The posy colours, up to five.
  static const posies = [
    Color(0xFFC96A5A),
    Color(0xFFD9B84E),
    Color(0xFF6F9CC9),
    Color(0xFF8FBF7F),
    Color(0xFFC97FA8),
  ];

  /// The ghost planting, when the game is asked why.
  static const ghost = Color(0xFFD9BE55);

  /// The armed chips, and the pointed-at bed.
  static const armed = Color(0xFFE0E4EA);
  static const shown = Color(0xFF8FB8D9);

  /// A garth bloomed, and one stranded.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
