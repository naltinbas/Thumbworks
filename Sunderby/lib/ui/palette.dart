import 'package:flutter/material.dart';

/// The sham's colours: a counting table at night, the shelf in slate,
/// the dots of the parts in chalk, odd parts warm and even cool, the
/// sum in gold.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const shelf = Color(0xFF3A3F55);
  static const shelfRim = Color(0xFF6C7BB0);
  static const rowBack = Color(0xFF2A2C34);
  static const oddDot = Color(0xFFE0A05A);
  static const evenDot = Color(0xFF7FB0DC);
  static const chalk = Color(0xFFECEAE2);
  static const gold = Color(0xFFE8C46A);
  static const short = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
