import 'dart:ui';

/// A wool yard at first light.
class Palette {
  const Palette._();

  /// The yard itself.
  static const yard = Color(0xFF211D18);

  /// Panels and cards: the barn boards.
  static const barn = Color(0xFF2E2820);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF453C2F);

  /// Borders that mean something.
  static const edge = Color(0xFF6B5C44);

  /// Words.
  static const ink = Color(0xFFEDE4D3);

  /// Words standing back.
  static const inkDim = Color(0xFF9C8F79);

  /// A bale of wool, and its darker underside.
  static const wool = Color(0xFFD9CBB0);
  static const woolShade = Color(0xFFB3A488);

  /// The weight inked on a bale.
  static const tod = Color(0xFF3A3226);

  /// The bale coming up the lane, and the cart it rides.
  static const cart = Color(0xFF87755A);

  /// The thread: the rising run drawn through the yard.
  static const thread = Color(0xFFE0B34E);

  /// The pointed-at place when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A morning at the fewest, and one gone over.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
