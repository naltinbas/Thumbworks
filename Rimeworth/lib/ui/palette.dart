import 'package:flutter/material.dart';

/// The colours.
///
/// A parish at four in the morning in a hard frost. The lanes are the colour
/// of wet tarmac until they are salted, and then they are the colour of grit
/// under headlights. The lorry is the only warm thing on the screen.
class Palette {
  const Palette._();

  static const night = Color(0xFF0B1015);
  static const verge = Color(0xFF151C24);
  static const line = Color(0xFF232D37);
  static const edge = Color(0xFF394654);

  static const lane = Color(0xFF303B46);
  static const salted = Color(0xFFAFC9DA);
  static const grit = Color(0xFF7B93A6);
  static const lorry = Color(0xFFE9A63C);

  static const ink = Color(0xFFE9EEF3);
  static const inkDim = Color(0xFF87919E);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFD4736B);
}
