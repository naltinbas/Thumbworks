import 'package:flutter/material.dart';

/// The colours.
///
/// A building site at first light. The timbers still on the ground are the
/// colour of sawn oak in shadow; the ones standing have the light on them.
class Palette {
  const Palette._();

  static const night = Color(0xFF10130F);
  static const verge = Color(0xFF1A1F19);
  static const line = Color(0xFF262D24);
  static const edge = Color(0xFF3C4638);

  static const down = Color(0xFF39413A);
  static const ready = Color(0xFF6E7A62);
  static const chosen = Color(0xFFD8B15E);
  static const up = Color(0xFFB08A4F);
  static const run = Color(0xFF7FB1C9);

  static const ink = Color(0xFFEBEEE9);
  static const inkDim = Color(0xFF889086);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
