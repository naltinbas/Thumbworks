import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF1A1817);
  static const board = Color(0xFF252220);
  static const line = Color(0xFF393430);

  /// The rack of pigeonholes.
  static const rack = Color(0xFF6E5A44);
  static const hole = Color(0xFF2C2622);

  /// The letters.
  static const paper = Color(0xFFE8E2D6);
  static const seal = Color(0xFFC96A4A);
  static const address = Color(0xFF3A342E);

  /// A letter home, called out.
  static const home = Color(0xFFC96A4A);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
