import 'package:flutter/material.dart';

/// The colours.
///
/// A harbour office at dusk with the book open on the desk. The ships waiting
/// are the colour of wet slate, the ones with the berth are lit like a window
/// with a lamp in it, and the hours that settle the day are the colour of the
/// water outside.
class Palette {
  const Palette._();

  static const night = Color(0xFF0B1116);
  static const verge = Color(0xFF141C23);
  static const line = Color(0xFF212B34);
  static const edge = Color(0xFF37444F);

  static const waiting = Color(0xFF3B4954);
  static const turned = Color(0xFF222B32);
  static const berthed = Color(0xFFE5A54B);
  static const tide = Color(0xFF6FC6DA);

  static const ink = Color(0xFFE8EEF3);
  static const inkDim = Color(0xFF87919C);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFDF6B60);
}
