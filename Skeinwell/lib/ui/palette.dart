import 'package:flutter/material.dart';

/// The sham's colours: five greens at dusk, the lanes in grass, a lane
/// that carries every stringing lit like a lamp.
class Palette {
  static const night = Color(0xFF121611);
  static const board = Color(0xFF1B2119);

  static const ink = Color(0xFFE9EDE2);
  static const inkDim = Color(0xFF919B8B);

  static const line = Color(0xFF3A452F);
  static const lane = Color(0xFF7FA36A);
  static const ghost = Color(0xFF3D4934);
  static const green = Color(0xFFD8C9A3);
  static const whole = Color(0xFFE0A33C);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
