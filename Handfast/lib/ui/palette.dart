import 'package:flutter/material.dart';

/// The colours.
///
/// A board chalked up at a hiring fair. The work is written down the side, the
/// hands across the top, and a cross where somebody can take something on.
class Palette {
  const Palette._();

  static const night = Color(0xFF101315);
  static const verge = Color(0xFF1A2024);
  static const line = Color(0xFF262E34);
  static const edge = Color(0xFF3A454E);

  static const cross = Color(0xFF75838E);
  static const taken = Color(0xFFCE9A45);
  static const dead = Color(0xFF313A41);
  static const shortOf = Color(0xFF7FB1C9);

  static const ink = Color(0xFFE9EDF0);
  static const inkDim = Color(0xFF868F97);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFD9736A);
}
