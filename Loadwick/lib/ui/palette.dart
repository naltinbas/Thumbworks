import 'package:flutter/material.dart';

/// The sham's colours: a fairground stall by lamplight, dice in bone
/// with ink pips, the house's die in madder, rolls won in green and
/// lost in rust on the table of thirty-six.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const stall = Color(0xFF3A2C22);
  static const cloth = Color(0xFF2E5A3A);
  static const bone = Color(0xFFF2EBD8);
  static const pip = Color(0xFF2A2420);
  static const madder = Color(0xFFB0523C);
  static const gold = Color(0xFFD9A441);
  static const won = Color(0xFF81C784);
  static const lost = Color(0xFFE57373);
  static const tie = Color(0xFF6E6A66);
  static const chalk = Color(0xFFECEAE2);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
