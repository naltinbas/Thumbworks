import 'package:flutter/material.dart';

/// The sham's colours: a chequered board of oak and walnut, knights in
/// brass, the pairing faint in chalk, rust where two knights attack
/// and green where every one stands unattacked.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const frame = Color(0xFF3A2A1E);
  static const light = Color(0xFFC9A87A);
  static const dark = Color(0xFF6E4A2E);
  static const brass = Color(0xFFD9A441);
  static const brassDark = Color(0xFF6B4A12);
  static const chalk = Color(0xFFECEAE2);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
