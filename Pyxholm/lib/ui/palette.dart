import 'package:flutter/material.dart';

/// The colours.
///
/// A counting house at night: dark oak, a brass beam, and coins the colour of
/// worn silver.
class Palette {
  const Palette._();

  static const night = Color(0xFF0E1114);
  static const verge = Color(0xFF181D22);
  static const line = Color(0xFF242C32);
  static const edge = Color(0xFF39434C);

  static const beam = Color(0xFF8E7A4A);
  static const coin = Color(0xFF9BA6AF);
  static const cleared = Color(0xFF2A3238);
  static const onLeft = Color(0xFF5E93C4);
  static const onRight = Color(0xFFC48A5E);

  static const ink = Color(0xFFE9EDF0);
  static const inkDim = Color(0xFF858E96);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFD9736A);
}
