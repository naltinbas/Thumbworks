import 'dart:ui';

/// A pasture under a high sky.
class Palette {
  const Palette._();

  /// Beyond the walls.
  static const moor = Color(0xFF171C15);

  /// The field, two grasses so the paces can be counted.
  static const grass = Color(0xFF232B1E);
  static const grassLight = Color(0xFF283122);

  /// The dry-stone wall round the field, and quieter lines.
  static const wall = Color(0xFF5C594A);
  static const line = Color(0xFF39412F);

  /// Panels and cards.
  static const byre = Color(0xFF262E20);

  /// Words.
  static const ink = Color(0xFFE9E6D5);

  /// Words standing back.
  static const inkDim = Color(0xFF98A088);

  /// The ewe, her shading, and her markings.
  static const fleece = Color(0xFFE3DCC8);
  static const fleeceShade = Color(0xFFB9B29D);
  static const muzzle = Color(0xFF41403A);

  /// The pen's hurdles.
  static const pen = Color(0xFF8A7754);

  /// The ladder: the safe squares, when the game is asked why.
  static const rung = Color(0xFFD9BE55);

  /// The pointed-at square when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// The pinder's last push.
  static const pinder = Color(0xFFC98F4E);

  /// A drive still at the fee, and one lost to the pinder.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
