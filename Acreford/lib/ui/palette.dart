import 'package:flutter/material.dart';

/// The field's colours: wood rails on a green night, gold for
/// the posts the paddock holds, rust for the ones it catches
/// mid-rail.
class Palette {
  static const night = Color(0xFF111710);
  static const board = Color(0xFF1C241A);

  static const ink = Color(0xFFE8EDDF);
  static const inkDim = Color(0xFF9AA48F);

  static const line = Color(0xFF39432F);
  static const post = Color(0xFF77816B);
  static const walked = Color(0xFFC8B37E);
  static const rail = Color(0xFFA67C4A);
  static const wash = Color(0x3381C784);

  static const held = Color(0xFFE0C25A);
  static const caught = Color(0xFFC97B5A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
