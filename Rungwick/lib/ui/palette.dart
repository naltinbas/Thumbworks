import 'package:flutter/material.dart';

/// The colours.
///
/// Rope and dark wood, with one warm colour for the rung being changed and
/// two for the only two things the game ever says: that is a word, or it is
/// not.
class Palette {
  const Palette._();

  static const night = Color(0xFF17161A);
  static const board = Color(0xFF211F26);
  static const rung = Color(0xFF2E2B36);
  static const rungEdge = Color(0xFF3B3746);

  static const rope = Color(0xFFC89B5A);
  static const good = Color(0xFF7BAE6F);
  static const bad = Color(0xFFD2685E);

  static const ink = Color(0xFFEAE7E2);
  static const inkDim = Color(0xFF948E9B);
}
