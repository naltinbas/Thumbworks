import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF191817);
  static const board = Color(0xFF242220);
  static const line = Color(0xFF383531);

  /// The rack and the weights.
  static const rackWood = Color(0xFF6E5A44);
  static const weight = Color(0xFF8A8578);
  static const weightChosen = Color(0xFFB8B2A4);
  static const weightInk = Color(0xFF2A2620);

  /// The beam and a balance found.
  static const beam = Color(0xFFE8B84B);
  static const clash = Color(0xFFC96A4A);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
