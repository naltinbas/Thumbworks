import 'package:flutter/material.dart';

/// A field at dusk: the ground dark, the plots in turned earth, and the
/// pegs painted red, blue and green by the colouring the proof needs.
class Palette {
  static const night = Color(0xFF10120F);
  static const board = Color(0xFF191C18);

  static const ink = Color(0xFFE9EBE4);
  static const inkDim = Color(0xFF8F958A);

  static const line = Color(0xFF343A31);
  static const ground = Color(0xFF1D2119);
  static const fence = Color(0xFF6A7360);

  static const plot = Color(0xFF8A6F45);
  static const plotEdge = Color(0xFFC7A468);

  static const red = Color(0xFFD1604A);
  static const blue = Color(0xFF5B92C9);
  static const green = Color(0xFF79B265);

  static const shown = Color(0xFF6FB0DE);
  static const good = Color(0xFF79B265);
  static const bad = Color(0xFFD1604A);
}
