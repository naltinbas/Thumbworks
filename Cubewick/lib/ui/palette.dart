import 'package:flutter/material.dart';

/// The sham's colours: a hexagon of triangles on slate, lozenges as
/// the three faces of a cube, top light, left mid, right dark, and a
/// chip out of the box in night.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF1F2127);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF383D45);
  static const slate = Color(0xFF2A2F36);
  static const grid = Color(0xFF3E4650);
  static const rim = Color(0xFF6E7684);
  static const top = Color(0xFFE9D9A8);
  static const left = Color(0xFFB98A4C);
  static const right = Color(0xFF7A5230);
  static const seam = Color(0xFF3B2A18);
  static const held = Color(0xFF64B5F6);
  static const bareTri = Color(0xFFE57373);
  static const chip = Color(0xFF101114);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
