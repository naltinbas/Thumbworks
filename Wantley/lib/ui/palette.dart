import 'package:flutter/material.dart';

/// The green's colours: whitewashed farms on a night field,
/// worn earth for the trodden paths, gold for a landed wish.
class Palette {
  static const night = Color(0xFF121711);
  static const board = Color(0xFF1D241B);

  static const ink = Color(0xFFEAEDDF);
  static const inkDim = Color(0xFF9CA48F);

  static const line = Color(0xFF39432F);
  static const faint = Color(0xFF2E3728);
  static const path = Color(0xFF9C7E52);
  static const farm = Color(0xFFE7DFC8);
  static const roof = Color(0xFFB4614A);

  static const met = Color(0xFF81C784);
  static const over = Color(0xFFE57373);
  static const wishGold = Color(0xFFE0BE52);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
