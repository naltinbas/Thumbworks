import 'package:flutter/material.dart';

/// The sham's colours: a baize table, cards in red, black and green,
/// blocks bracketed gold when mixed and rust when not.
class Palette {
  static const night = Color(0xFF0F1512);
  static const board = Color(0xFF19241E);

  static const ink = Color(0xFFECF1EC);
  static const inkDim = Color(0xFF9DAAA1);

  static const line = Color(0xFF2F4037);
  static const baize = Color(0xFF1B3327);
  static const baizeRim = Color(0xFF3B5A48);
  static const cardFace = Color(0xFFF3EEE2);
  static const cardEdge = Color(0xFFB9B2A4);
  static const red = Color(0xFFC94A3B);
  static const black = Color(0xFF23262B);
  static const green = Color(0xFF3F9A5B);
  static const mixed = Color(0xFFD9B44A);
  static const unmixed = Color(0xFFE57373);
  static const empty = Color(0xFF243A2E);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
