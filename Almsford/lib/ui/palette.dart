import 'package:flutter/material.dart';

/// The sham's colours: an almshouse yard, grain in barley gold, the
/// bins in weathered oak, the measure in hand lit.
class Palette {
  static const night = Color(0xFF15140F);
  static const board = Color(0xFF201E16);

  static const ink = Color(0xFFEDE8D9);
  static const inkDim = Color(0xFF98917E);

  static const line = Color(0xFF41392A);
  static const bin = Color(0xFF6B5B41);
  static const grain = Color(0xFFD9B25A);
  static const held = Color(0xFFF0DCA0);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
