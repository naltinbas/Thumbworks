import 'package:flutter/material.dart';

/// A hoop of lamps at night: the sockets cold, the lit lamps warm, the
/// dark stones slate and the pale stones chalk.
class Palette {
  static const night = Color(0xFF0F1114);
  static const board = Color(0xFF181B20);

  static const ink = Color(0xFFE8EAEE);
  static const inkDim = Color(0xFF8D939C);

  static const line = Color(0xFF313740);
  static const hoop = Color(0xFF39404A);
  static const socket = Color(0xFF4A515C);

  static const dark = Color(0xFF3E4756);
  static const darkRim = Color(0xFF8894A6);
  static const pale = Color(0xFFDCD8CB);

  static const unlit = Color(0xFF1C2026);
  static const lit = Color(0xFFE8B54A);

  static const walk = Color(0xFF6FB0DE);
  static const shown = Color(0xFF6FB0DE);
  static const good = Color(0xFF83BE79);
  static const bad = Color(0xFFD4715E);
}
