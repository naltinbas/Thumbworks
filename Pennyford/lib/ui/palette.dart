import 'package:flutter/material.dart';

/// The sham's colours: a table at night, the middle coin in bronze, the
/// ring coins in silver, and the turn to spare in red.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const middle = Color(0xFFD1A85E);
  static const middleFace = Color(0xFF3E3324);
  static const ring = Color(0xFFB9BCC4);
  static const ringFace = Color(0xFF3A3D45);
  static const spare = Color(0xFFE0645A);
  static const spoke = Color(0xFF3B3947);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
