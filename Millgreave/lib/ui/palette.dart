import 'dart:ui';

/// A high moor under a working sky.
class Palette {
  const Palette._();

  /// Beyond the moor.
  static const sky = Color(0xFF181B1D);

  /// Panels and cards: the barn boards.
  static const barn = Color(0xFF25292B);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3A4145);

  /// Borders that mean something.
  static const edge = Color(0xFF5D6B72);

  /// Words.
  static const ink = Color(0xFFEAEDEA);

  /// Words standing back.
  static const inkDim = Color(0xFF97A29C);

  /// The two heathers of the plots.
  static const heath = Color(0xFF2E3226);
  static const heathLight = Color(0xFF383D2E);

  /// A windmill: post, sails, and cap.
  static const post = Color(0xFF8A7457);
  static const sail = Color(0xFFD9CFB8);
  static const cap = Color(0xFF6E5C44);

  /// The ghost mills of the built rows, when the game is asked why.
  static const ghost = Color(0xFFD9BE55);

  /// The pointed-at plot when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A moor set, and one stranded.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
