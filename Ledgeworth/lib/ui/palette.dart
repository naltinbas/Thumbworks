import 'package:flutter/material.dart';

/// The sham's colours: a desk in oak, books in cloth bindings, the
/// balance marked green where the stack stands and rust where it
/// topples.
class Palette {
  static const night = Color(0xFF17161B);
  static const board = Color(0xFF232129);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const desk = Color(0xFF7A5230);
  static const deskEdge = Color(0xFF4A3119);
  static const floor = Color(0xFF1E1C22);
  static const spine = Color(0xFFD9A441);
  static const pages = Color(0xFFEFE6D0);
  static const stands = Color(0xFF81C784);
  static const topples = Color(0xFFE57373);
  static const ruler = Color(0xFF9A9C93);

  /// The books' cloth, one each from the top.
  static const cloth = [
    Color(0xFF8B3A3A),
    Color(0xFF2F5D8A),
    Color(0xFF3F7A4A),
    Color(0xFF7A5A9A),
    Color(0xFFB0762E),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
