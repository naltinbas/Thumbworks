import 'package:flutter/material.dart';

/// The colours.
///
/// A dark country with beacons on it. A hill that is lit is warm, a hill
/// nothing can see is cold and dim, and the sightlines are faint until a
/// beacon is using one.
class Palette {
  const Palette._();

  static const night = Color(0xFF0F1216);
  static const moor = Color(0xFF181D23);
  static const line = Color(0xFF2B333C);
  static const edge = Color(0xFF3E4954);

  static const dark = Color(0xFF3A434D);
  static const lit = Color(0xFFE0A45C);
  static const beacon = Color(0xFFF6D9A6);
  static const reach = Color(0xFF6E5C43);

  static const ink = Color(0xFFEBEEF2);
  static const inkDim = Color(0xFF8B939C);
  static const good = Color(0xFF7FB86D);
  static const bad = Color(0xFFD4736B);
}
