import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF181917);
  static const board = Color(0xFF232421);
  static const line = Color(0xFF363832);

  /// The cottages.
  static const wall = Color(0xFFCFC4AE);
  static const roof = Color(0xFF8A5A44);
  static const window = Color(0xFFE8B84B);

  /// The wires.
  static const wire = Color(0xFF5BB98C);

  /// A loop of wire, called out.
  static const loop = Color(0xFFC96A4A);

  /// A lane's end, lit.
  static const lanesEnd = Color(0xFFE8B84B);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
