import 'package:flutter/material.dart';

/// The sham's colours: the inn's billiard room, green baize in a wooden
/// frame, the ball's path in chalk, the pocket found in gold.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const room = Color(0xFF26222B);
  static const baize = Color(0xFF2E6B3F);
  static const baizeLine = Color(0xFF357A49);
  static const frame = Color(0xFF5B3A1E);
  static const pocket = Color(0xFF17151A);
  static const pocketFound = Color(0xFFE9C766);
  static const path = Color(0xFFECEAE2);
  static const bounce = Color(0xFFF2B33D);
  static const ball = Color(0xFFF7F3E8);
  static const home = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
