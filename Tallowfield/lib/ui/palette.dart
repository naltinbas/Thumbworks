import 'dart:ui';

/// A walled garden after dusk.
class Palette {
  const Palette._();

  /// The dark of the garden.
  static const dusk = Color(0xFF16191A);

  /// Panels and cards: the wall stones.
  static const wall = Color(0xFF232827);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF39413D);

  /// Borders that mean something.
  static const edge = Color(0xFF5C6B60);

  /// Words.
  static const ink = Color(0xFFEAECE0);

  /// Words standing back.
  static const inkDim = Color(0xFF95A093);

  /// The three hedges, told apart by their greens.
  static const hedgeA = Color(0xFF5E7D52);
  static const hedgeB = Color(0xFF4F7D77);
  static const hedgeC = Color(0xFF7D7A4C);

  /// A lantern lit, its glow, and one gone dark.
  static const flame = Color(0xFFF2C24E);
  static const glow = Color(0x33F2C24E);
  static const lantern = Color(0xFF8A8072);
  static const dark = Color(0xFF3E3B34);

  /// A hedge's complaint.
  static const complaint = Color(0xFFD98A4E);

  /// The pointed-at lantern when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// An evening read true, and a reading slipped.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
