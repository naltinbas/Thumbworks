import 'package:flutter/material.dart';

/// The colours of the game: floorboards in a bare house.
class Palette {
  static const house = Color(0xFF161310);
  static const panel = Color(0xFF221D17);
  static const line = Color(0xFF3E362B);
  static const edge = Color(0xFF5C513F);
  static const ink = Color(0xFFF0EBE0);
  static const inkDim = Color(0xFFA89F8E);

  static const bare = Color(0xFF241F19);
  static const bareEdge = Color(0xFF3A3328);
  static const plank = Color(0xFFC08B4E);
  static const plankGrain = Color(0xFF9C6E3C);

  /// The two colours of the floor, shown when asked.
  static const darkTint = Color(0x5533261A);
  static const lightTint = Color(0x22E8DCC2);

  static const armed = Color(0xFF64B6F0);
  static const shown = Color(0xFF64B6F0);

  static const good = Color(0xFF8FBD8F);
  static const bad = Color(0xFFD9705C);
}
