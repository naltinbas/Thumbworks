import 'package:flutter/material.dart';

/// The sham's colours: the moot hall at night, hamlets in their own
/// dyes, quotas as hollow bars and seats as filled benches.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const hall = Color(0xFF26222B);
  static const seat = Color(0xFFD9B75A);
  static const hamlets = [
    Color(0xFF9CC5E8),
    Color(0xFFA9D3A0),
    Color(0xFFF0A9A0),
    Color(0xFFCDB4E5),
  ];
  static const quota = Color(0xFF3F3A47);
  static const bench = Color(0xFF5B3A1E);
  static const lost = Color(0xFFE57373);
  static const gained = Color(0xFF81C784);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
