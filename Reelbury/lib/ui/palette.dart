import 'package:flutter/material.dart';

/// The colours.
///
/// A hall by lamplight. One side warm and one side cool, so a glance says
/// which side a name is on without reading it; a couple is a line between
/// them, and the only red on the screen is a pair who would rather swap.
class Palette {
  const Palette._();

  static const night = Color(0xFF141118);
  static const floor = Color(0xFF1E1A24);
  static const board = Color(0xFF272232);
  static const edge = Color(0xFF3A3348);

  static const caller = Color(0xFFE0A05F);
  static const callerEdge = Color(0xFFF2C793);
  static const dancer = Color(0xFF79BBD2);
  static const dancerEdge = Color(0xFFB5E0EE);

  static const tie = Color(0xFF9C8FC4);
  static const swap = Color(0xFFD4736B);

  static const ink = Color(0xFFEDE9F2);
  static const inkDim = Color(0xFF8F8899);
  static const good = Color(0xFF86BE77);
  static const bad = Color(0xFFD4736B);
}
