import 'package:flutter/material.dart';

/// The sham's colours: a counting hall at night, Ash's ballots in gold,
/// Birch's in silver, the lead line green above and rust below, the
/// level line chalk.
class Palette {
  static const night = Color(0xFF15161A);
  static const board = Color(0xFF20222A);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const grid = Color(0xFF2A2C36);
  static const levelLine = Color(0xFF6B6570);
  static const ash = Color(0xFFD1A85E);
  static const birch = Color(0xFFB9BCC4);
  static const ahead = Color(0xFF81C784);
  static const behind = Color(0xFFE0645A);
  static const levelDot = Color(0xFFECEAE2);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
