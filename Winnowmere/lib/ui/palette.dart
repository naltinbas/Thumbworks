import 'package:flutter/material.dart';

/// The colours.
///
/// A wire frame on a dark bench. The lines are pale, a comparator is a rung
/// between two of them, and the only warm colour on the screen is the row of
/// noughts and ones that is still coming out wrong.
class Palette {
  const Palette._();

  static const night = Color(0xFF121417);
  static const bench = Color(0xFF1B1F24);
  static const rail = Color(0xFF293037);
  static const edge = Color(0xFF3D4652);

  static const line = Color(0xFF6F7C8C);
  static const rung = Color(0xFF7FC4D8);
  static const given = Color(0xFF4A5A6B);
  static const wrong = Color(0xFFE0A05F);

  static const ink = Color(0xFFE9EDF1);
  static const inkDim = Color(0xFF8A94A0);
  static const good = Color(0xFF7FC28C);
  static const bad = Color(0xFFD4736B);
}
