import 'package:flutter/material.dart';

/// The sham's colours: a felt table, bone counters, the chosen one
/// in gold, the holding column washed warm, the asked place ringed.
class Palette {
  static const night = Color(0xFF12140F);
  static const board = Color(0xFF1C2018);

  static const ink = Color(0xFFEDEFE6);
  static const inkDim = Color(0xFF9EA595);

  static const line = Color(0xFF353B2E);
  static const felt = Color(0xFF243024);
  static const feltRim = Color(0xFF465A46);
  static const counter = Color(0xFFE6DCC4);
  static const counterInk = Color(0xFF3B3320);
  static const chosen = Color(0xFFD9B44A);
  static const holding = Color(0xFF3B4A2E);
  static const asked = Color(0xFF64B5F6);
  static const placing = Color(0xFF2E3A2E);
  static const placingInk = Color(0xFFDDE4D6);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
