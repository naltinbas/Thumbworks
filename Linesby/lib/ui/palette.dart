import 'package:flutter/material.dart';

/// The sham's colours: a pegged field at night, the triangle in chalk,
/// the centroid green, the circumcentre blue, the orthocentre rust, and
/// the Euler line gold through them.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const peg = Color(0xFF3E3E4E);
  static const chalk = Color(0xFFD9D4C4);
  static const chalkDim = Color(0xFF6E6A62);
  static const held = Color(0xFFF2E6B8);
  static const centroid = Color(0xFF7CC47F);
  static const circum = Color(0xFF6FA8DC);
  static const ortho = Color(0xFFD9695A);
  static const euler = Color(0xFFE8C46A);
  static const circle = Color(0xFF2E3A4A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
