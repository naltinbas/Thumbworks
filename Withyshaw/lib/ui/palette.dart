import 'dart:ui';

/// A hedgerow lane in March.
class Palette {
  const Palette._();

  /// The lane behind everything.
  static const lane = Color(0xFF171A15);

  /// Panels and cards: the gate boards.
  static const gate = Color(0xFF242820);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3A4133);

  /// Borders that mean something.
  static const edge = Color(0xFF5F6B52);

  /// Words.
  static const ink = Color(0xFFECEEE2);

  /// Words standing back.
  static const inkDim = Color(0xFF9AA38E);

  /// Your withies, and the hedger's.
  static const yours = Color(0xFF6F9CC9);
  static const yoursDeep = Color(0xFF547EA6);
  static const theirs = Color(0xFFB56A4A);
  static const theirsDeep = Color(0xFF925034);

  /// The ground the stalks spring from.
  static const ground = Color(0xFF4A4433);

  /// The worths, when the game is asked why.
  static const worth = Color(0xFFD9BE55);

  /// The pointed-at withy when the game is asked to show.
  static const shown = Color(0xFFE0E4EA);

  /// A hedge held, and one lost.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
