import 'package:flutter/material.dart';

/// The sham's colours: a fairground board with the bouts chalked in
/// a table, the line-up on sawdust, links green where the throw
/// holds and rust where it breaks.
class Palette {
  static const night = Color(0xFF17161B);
  static const board = Color(0xFF232129);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const slate = Color(0xFF2B2F2C);
  static const chalk = Color(0xFFE9E4D4);
  static const chalkDim = Color(0xFF8E8A7C);
  static const sawdust = Color(0xFF3A3226);
  static const slot = Color(0xFF4A4033);
  static const brass = Color(0xFFD9A441);
  static const held = Color(0xFF81C784);
  static const broke = Color(0xFFE57373);

  /// The wrestlers' colours, one each.
  static const wrestlers = [
    Color(0xFF6C86B0),
    Color(0xFFB0523C),
    Color(0xFF7BA05B),
    Color(0xFFC49A3C),
    Color(0xFF9C6BAF),
    Color(0xFF5FA8A0),
  ];

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
