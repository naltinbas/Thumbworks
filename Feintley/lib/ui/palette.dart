import 'package:flutter/material.dart';

/// The sham's colours: a test bench at night, the squares in copper,
/// the ones the power takes in gold, the stamp green when the number
/// passes and red when it fails.
class Palette {
  static const night = Color(0xFF141418);
  static const board = Color(0xFF1F1F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const bench = Color(0xFF26262F);
  static const benchRim = Color(0xFF3E3E4E);
  static const chalk = Color(0xFFD9D4C4);
  static const copper = Color(0xFFC98E5A);
  static const gold = Color(0xFFE8C46A);
  static const face = Color(0xFF3A2E12);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
