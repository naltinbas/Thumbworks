import 'package:flutter/material.dart';

/// A yard at dawn: the near row in dark hide, the off row in dun, the
/// yokes in timber, and a crossed pair marked in rust.
class Palette {
  static const night = Color(0xFF13120E);
  static const board = Color(0xFF1D1B15);

  static const ink = Color(0xFFEBE6D9);
  static const inkDim = Color(0xFF968D7C);

  static const line = Color(0xFF39352B);
  static const yoke = Color(0xFF6E6046);
  static const nearOx = Color(0xFF6F7A5C);
  static const offOx = Color(0xFFC9A96B);

  static const crossed = Color(0xFFC4694A);
  static const matched = Color(0xFF8FB073);

  static const shown = Color(0xFF6FA8D6);
  static const good = Color(0xFF8FB073);
  static const bad = Color(0xFFC4694A);
}
