import 'package:flutter/material.dart';

/// The sham's colours: a lane of milestones on grass, the marked run
/// in gold, its sum told in coins below.
class Palette {
  static const night = Color(0xFF15181A);
  static const board = Color(0xFF1F2528);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF344041);
  static const grass = Color(0xFF2E4A2C);
  static const lane = Color(0xFF6E5A3E);
  static const stone = Color(0xFFCFC7B4);
  static const stoneInk = Color(0xFF3B3327);
  static const marked = Color(0xFFD9A441);
  static const inRun = Color(0xFFF2D77A);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
  static const coin = Color(0xFFD9A441);
  static const coinEdge = Color(0xFF8A6A1F);

  static const shown = Color(0xFF64B5F6);
}
