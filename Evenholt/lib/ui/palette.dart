import 'package:flutter/material.dart';

/// The sham's colours: two trays on a dark cloth, tokens in
/// madder on the left and indigo on the right, green where a
/// power agrees and rust where it parts.
class Palette {
  static const night = Color(0xFF12141A);
  static const board = Color(0xFF1D2028);

  static const ink = Color(0xFFE9EAF0);
  static const inkDim = Color(0xFF9CA0AE);

  static const line = Color(0xFF363A48);
  static const tray = Color(0xFF232732);
  static const trayRim = Color(0xFF474D60);

  static const token = Color(0xFFB8674E);
  static const tokenRight = Color(0xFF4F6FA8);
  static const tokenInk = Color(0xFFF4EEE6);

  static const agree = Color(0xFF81C784);
  static const part = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
