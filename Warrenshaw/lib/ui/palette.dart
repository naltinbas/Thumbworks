import 'package:flutter/material.dart';

/// The colours.
///
/// Dusk over fields. The paths are pale ruts in dark grass; the seeker is the
/// one warm thing on the map and the runner is the one cold one, so a glance
/// says which is which without reading anything.
class Palette {
  const Palette._();

  static const night = Color(0xFF10130F);
  static const field = Color(0xFF181D17);
  static const hedge = Color(0xFF232B22);
  static const path = Color(0xFF3A4638);
  static const edge = Color(0xFF4A5847);

  static const place = Color(0xFF2A332A);
  static const placeEdge = Color(0xFF3E4B3D);

  static const seeker = Color(0xFFE0A35C);
  static const seekerEdge = Color(0xFFF3CE9B);
  static const runner = Color(0xFF74C6D6);
  static const runnerEdge = Color(0xFFB6E7F1);

  static const ink = Color(0xFFE9EDE7);
  static const inkDim = Color(0xFF8E9A8C);
  static const good = Color(0xFF89BE72);
  static const bad = Color(0xFFD4796B);
}
