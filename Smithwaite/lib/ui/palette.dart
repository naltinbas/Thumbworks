import 'dart:ui';

/// A smithy after the fire is banked.
class Palette {
  const Palette._();

  /// The room behind everything.
  static const soot = Color(0xFF1B1A18);

  /// Panels and cards: the bench boards.
  static const bench = Color(0xFF272420);

  /// Hairlines and quiet borders.
  static const line = Color(0xFF3F3930);

  /// Borders that mean something.
  static const edge = Color(0xFF6A5F4C);

  /// Words.
  static const ink = Color(0xFFECE7DA);

  /// Words standing back.
  static const inkDim = Color(0xFF9C9482);

  /// The bar the rings ride, and its darker underside.
  static const bar = Color(0xFF9BA1A8);
  static const barDark = Color(0xFF60666D);

  /// A ring, bright where it is yours to move and duller where the cords
  /// hold it.
  static const ring = Color(0xFFCBA75E);
  static const ringHeld = Color(0xFF87754F);

  /// The cords down to the plate, and the plate itself.
  static const cord = Color(0xFF5C4F3B);
  static const plate = Color(0xFF473C2C);

  /// The smith's figures, when the game is asked why.
  static const figure = Color(0xFFE3B94F);

  /// The pointed-at ring when the game is asked to show.
  static const shown = Color(0xFF8FB8D9);

  /// A bar freed at the fewest, and a count gone over.
  static const good = Color(0xFF8FBF7F);
  static const bad = Color(0xFFCC7A66);
}
