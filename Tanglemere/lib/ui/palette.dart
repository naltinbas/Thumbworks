import 'package:flutter/material.dart';

/// The colours of the game: a loom of threads after dark.
class Palette {
  static const dark = Color(0xFF131318);
  static const panel = Color(0xFF1E1E26);
  static const line = Color(0xFF383844);
  static const edge = Color(0xFF545464);
  static const ink = Color(0xFFEDECE4);
  static const inkDim = Color(0xFF9E9EAC);

  static const post = Color(0xFFC9B08A);
  static const postRim = Color(0xFF8F7B58);
  static const free = Color(0xFF34343E);
  static const mineThread = Color(0xFF4FA098);
  static const houseThread = Color(0xFFC46A55);

  static const shown = Color(0xFF64B6F0);
  static const closed = Color(0xFFE05545);

  static const good = Color(0xFF8FBD8F);
  static const bad = Color(0xFFD9705C);
}
