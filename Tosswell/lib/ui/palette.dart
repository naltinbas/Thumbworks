import 'package:flutter/material.dart';

/// The sham's colours: a fair after dark, the purse up in silver and
/// down in red, the standings you walk away from ringed in brass.
class Palette {
  static const night = Color(0xFF101418);
  static const board = Color(0xFF1A2028);

  static const ink = Color(0xFFE6EAEE);
  static const inkDim = Color(0xFF8B939D);

  static const line = Color(0xFF333C47);
  static const step = Color(0xFF46515F);
  static const up = Color(0xFF7FB08A);
  static const down = Color(0xFFC46A62);
  static const level = Color(0xFF6E7A88);
  static const marked = Color(0xFFD7B25C);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
