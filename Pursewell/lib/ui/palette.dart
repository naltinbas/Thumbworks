import 'package:flutter/material.dart';

/// Every colour in the game, and nowhere else.
class Palette {
  static const night = Color(0xFF1A1917);
  static const board = Color(0xFF252421);
  static const line = Color(0xFF393731);

  /// The coins and the tray.
  static const coin = Color(0xFFE8B84B);
  static const coinRim = Color(0xFF8A6D2B);
  static const coinInk = Color(0xFF3A2F17);
  static const tray = Color(0xFF2C2A24);

  /// Neighbouring coins in the tray, called out.
  static const neighbour = Color(0xFFC96A4A);

  /// The way already shown, on the hopeless purse.
  static const shownWay = Color(0xFF5BB98C);

  /// What the tools speak in.
  static const shown = Color(0xFF7EC8E3);
  static const good = Color(0xFF5BB98C);
  static const bad = Color(0xFFC96A4A);

  static const ink = Color(0xFFE8E2D6);
  static const inkDim = Color(0xFF9AA5AE);
}
