import 'package:flutter/material.dart';

/// The sham's colours: a green at harvest, the field in pasture, the
/// ricks in straw, the markers in barn red.
class Palette {
  static const night = Color(0xFF12160F);
  static const board = Color(0xFF1B2117);

  static const ink = Color(0xFFE9EEE0);
  static const inkDim = Color(0xFF8F9A84);

  static const line = Color(0xFF39442F);
  static const peg = Color(0xFF4B5740);
  static const field = Color(0xFF7FA36A);
  static const straw = Color(0xFFCBA95E);
  static const marker = Color(0xFFC4563F);
  static const ring = Color(0xFFE0A33C);
  static const misfit = Color(0xFFE0645A);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
