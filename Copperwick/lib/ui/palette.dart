import 'package:flutter/material.dart';

/// The sham's colours: an oak table by lamplight, the spots dimpled
/// into it, pennies in copper, the one in hand ringed gold, green where
/// the triangle has turned and rust where the moves ran out.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const table = Color(0xFF4A3323);
  static const tableEdge = Color(0xFF2E1F16);
  static const dimple = Color(0xFF3B281B);
  static const ghost = Color(0xFF7A5A3E);
  static const copper = Color(0xFFB8712F);
  static const copperRim = Color(0xFFE2A25E);
  static const copperShade = Color(0xFF7E4A1C);
  static const gold = Color(0xFFF2C94C);
  static const clash = Color(0xFFE57373);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
