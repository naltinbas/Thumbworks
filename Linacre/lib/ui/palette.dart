import 'package:flutter/material.dart';

/// The colours.
///
/// A telegraph line at dusk: sky nearly black, poles darker, the wire a thin
/// silver. Braced wire is warm brass, cut wire leaves red-raw stubs, and the
/// two webs, when the game shows them, are the two cool tints.
class Palette {
  const Palette._();

  static const night = Color(0xFF0C0F14);
  static const verge = Color(0xFF161B22);
  static const line = Color(0xFF222A34);
  static const edge = Color(0xFF39434F);

  static const wire = Color(0xFF7C8894);
  static const braced = Color(0xFFD1A554);
  static const stub = Color(0xFFB05B50);
  static const post = Color(0xFF4A4238);
  static const station = Color(0xFF6E7B86);

  static const webOne = Color(0xFF6FA8CF);
  static const webOther = Color(0xFFC98AD6);

  static const ink = Color(0xFFE9EDF2);
  static const inkDim = Color(0xFF87909B);
  static const good = Color(0xFF8CBB6D);
  static const bad = Color(0xFFD9736A);
}
