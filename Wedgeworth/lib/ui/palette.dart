import 'package:flutter/material.dart';

/// The sham's colours: a paper-craft bench at night, faces in cream and
/// soft paper tones, the gap in green and the overlap in rust.
class Palette {
  static const night = Color(0xFF17151A);
  static const board = Color(0xFF231F27);

  static const ink = Color(0xFFECEAE2);
  static const inkDim = Color(0xFF9A9C93);

  static const line = Color(0xFF3B3947);
  static const bench = Color(0xFF26222B);
  static const paper = Color(0xFFF1E9D2);
  static const papers = [
    Color(0xFFF1E9D2),
    Color(0xFF9CC5E8),
    Color(0xFFF0A9A0),
    Color(0xFFE9C766),
    Color(0xFFA9D3A0),
    Color(0xFFCDB4E5),
    Color(0xFFF3C79A),
    Color(0xFF9FD8CF),
  ];
  static const fold = Color(0xFF3B3226);
  static const gap = Color(0xFF6BBF7A);
  static const over = Color(0xFFD9645A);
  static const dial = Color(0xFF2E2A34);
  static const dialOn = Color(0xFFE9C766);

  static const shown = Color(0xFF64B5F6);
  static const good = Color(0xFF81C784);
  static const bad = Color(0xFFE57373);
}
